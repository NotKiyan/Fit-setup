import React from 'react';

// You can pass { user } as a prop later when you need it for API calls
export default function ProgressAnalytics({ user }) {
    return (
        <div className="profile-section">
        <div className="section-header">
        <h3>Progress & Analytics</h3>
        <p>Visualize your hard work.</p>
        </div>

        {/* You will build the charts and PR list here */}
        <p>Progress Analytics Component - Coming Soon!</p>

        </div>
    );
}
